spring:
  application:
    name: ecommerce
  datasource:
    url: jdbc:mysql://localhost:3306/practiceecom
    username: root
    password: MyPassword!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
